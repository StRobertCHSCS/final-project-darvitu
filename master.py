# load the main class
from main import Main

# run main Although all code according to the document must be in master.py, our code is too complicated to be in one
# file but since this Main() function calls all the other classes from the other files, technically its still all in
# master.py
Main()
